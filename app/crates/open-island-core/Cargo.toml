[package]
name = "open-island-core"
version.workspace = true
edition.workspace = true

[lib]
name = "open_island_core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

libc = "0.2"

[build-dependencies]
cc = "1"
