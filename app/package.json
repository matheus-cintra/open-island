{
  "name": "app",
  "private": true,
  "version": "0.4.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "bun test --isolate",
    "tauri": "tauri",
    "prebundle": "cargo build --release -p open-islandd && node scripts/stage-sidecar.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/bun": "^1.4.1",
    "happy-dom": "^20.14.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
